<h1>Welcome to the students pages</h1>
<%= link_to "new student", new_student_path %>
<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student) %></div>
  <% end %>
</div>
